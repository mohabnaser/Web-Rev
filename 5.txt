# Slide 1: Title
IS231: Web Technology
JavaScript-1
By: Neamat El Tazi

---

# Slide 2: References
2
Slides from Carlton University:
http://cglab.ca/~morin/teaching/2405/notes/javascript1.pdf
W3schools.com
Web Technology - Neamat El Tazi

---

# Slide 3: Outline
3
Outline
- What is JavaScript?
- History
- Uses
- Adding JavaScript to HTML
- JavaScript syntax
- JavaScript events
- JavaScript classes
- The HTML Document Object Model
Web Technology - Neamat El Tazi

---

# Slide 4: What is JavaScript?
- JavaScript is a programming language for use in HTML pages
- Invented in 1995 at Netscape Corporation (LiveScript)
- JavaScript has nothing to do with Java
- JavaScript programs are run by an interpreter built into the user's web browser (not on the server)
4
Web Technology - Neamat El Tazi

---

# Slide 5: Where does JavaScript Fit In?
- Recall
  1. client opens connection to server
  2. client sends request to server
  3. server sends response to client
  4. client and server close connection
- What about Step 5?
  5. Client renders (displays) the response received from server
- Step 5 involves displaying HTML
- And running any JavaScript code within the HTML
5
Web Technology - Neamat El Tazi

---

# Slide 6: What can JavaScript Do?
- JavaScript can dynamically modify an HTML page
- JavaScript can react to user input
- JavaScript can validate user input
- JavaScript can be used to create cookies (yum!)
- JavaScript is a full-featured programming language
- JavaScript user interaction does not require any communication with the server
6
Web Technology - Neamat El Tazi

---

# Slide 7: Pros and Cons of JavaScript
- Pros:
  - Allows more dynamic HTML pages, even complete web applications
- Cons:
  - Requires a JavaScript-enabled browser
  - Requires a client who trusts the server enough to run the code the server provides
  - JavaScript has some protection in place but can still cause security problems for clients
- Newest Version: ECMAScript 2025
7
Web Technology - Neamat El Tazi

---

# Slide 8: Using JavaScript in your HTML
JavaScript can be inserted into documents by using the SCRIPT tag

Code Example:
<html>
<head>
<title>Hello World in JavaScript</title>
</head>
<body>
<script type="text/javascript">
document.write("Hello World!");
</script>
</body>
</html>

---

# Slide 9: Where to Put your Scripts
- You can have any number of scripts
- Scripts can be placed in the HEAD or in the BODY
  - In the HEAD, scripts are run before the page is displayed
  - In the BODY, scripts are run as the page is displayed
- In the HEAD is the right place to define functions and variables that are used by scripts within the BODY
9
Web Technology - Neamat El Tazi

---

# Slide 10: Using JavaScript in your HTML
Code Example:
<html>
<head>
<title>Hello World in JavaScript</title>
<script type="text/javascript">
function helloWorld() {
  document.write("Hello World!");
}
</script>
</head>
<body>
<script type="text/javascript">
helloWorld();
</script>
</body>
</html>
10
Web Technology - Neamat El Tazi

---

# Slide 11: External Scripts
Check Demo: 00.html

[Image Description: A slide titled "External Scripts"]
- Scripts can also be loaded from an external file
- This is useful if you have a complicated script or set of subroutines that are used in several different documents

Code Example:
<script src="myscript.js"></script>

Web Technology - Neamat El Tazi 11

---

# Slide 12: JavaScript Variables
[Image Description: A slide titled "JavaScript Variables"]
- JavaScript has variables that you can declare with the optional var keyword
- Variables declared within a function are local to that function
- Variables declared outside of any function are global variables

Code Example:
var myname = "Pat Morin";

Web Technology - Neamat El Tazi 12

---

# Slide 13: JavaScript Operators and Constructs
[Image Description: A slide titled "JavaScript Operators and Constructs"]
- JavaScript has most of the operators we're used to from C/Java
  - Arithmetic (+, -, *, /, %)
  - Assignment (=, +=, -=, *=, /=, %=, ++, --)
  - Logical (&&, ||, !)
  - Comparison (<, >, <=, >=, ==)
- Note: + also does string concatenation
- Constructs:
  - if, else, while, for, switch, case

Web Technology - Neamat El Tazi 13

---

# Slide 14: Simple User Interaction
Check Demo: 0.html

[Image Description: A slide titled "Simple User Interaction"]
- There are three built-in methods of doing simple user interaction
  - alert(msg) alerts the user that something has happened
  - confirm(msg) asks the user to confirm (or cancel) something
  - prompt(msg, default) asks the user to enter some text

Code Example:
alert("There's a monster on the wing!");
confirm("Are you sure you want to do that?");
prompt("Enter you name", "Adam");

Web Technology - Neamat El Tazi 14

---

# Slide 15: JavaScript Functions
[Image Description: A slide titled "JavaScript Functions"]
- JavaScript lets you define functions using the function keyword
- Functions can return values using the return keyword

Code Example:
function showConfirm() {
  confirm("Are you sure you want to do that?");
}

Web Technology - Neamat El Tazi 15

---

# Slide 16: JavaScript Arrays
[Image Description: A slide titled "JavaScript Arrays"]
- JavaScript has arrays that are indexed starting at 0
- Special version of for works with arrays

Code Example:
<script type="text/javascript">
var colors = new Array();
colors[0] = "red"; colors[1] = "green";
colors[2] = "blue"; colors[3] = "orange";
colors[4] = "magenta"; colors[5] = "cyan";
for (var i in colors) {
  document.write("<div style=\"background-color:"
               + colors[i] + ";\">"
               + colors[i] + "</div>\n");
}
</script>

Web Technology - Neamat El Tazi 16

---

# Slide 17: JavaScript Events
Check Demo: 1.html
Check Demo: 2.html

[Image Description: A slide titled "JavaScript Events"]
- JavaScript can be made to respond to user events
- Common Events:
  - onload and onunload : when a page is first visited or left
  - onfocus, onblur, onchange : events pertaining to form elements
  - onsubmit : when a form is submitted
  - onmouseover, onmouseout : for "menu effects"
- A complete list of event types is available here
  - http://www.w3schools.com/jsref/jsref_events.asp

Web Technology - Neamat El Tazi 17

---

# Slide 18: Exception Handling
[Image Description: A slide titled "Exception Handling"]
- JavaScript also has try, catch, and throw keywords for handling JavaScript errors

Code Example:
try {
  runSomeCode();
} catch(err) {
  var txt="There was an error on this page.\n\n"
        + "Error description: "
        + err.description + "\n\n"
  alert(txt)
}

Web Technology - Neamat El Tazi 18

---

# Slide 19: Comments in JavaScript
[Image Description: A slide titled "Comments in JavaScript"]
- Comments in JavaScript are delimited with // and /* */ as in Java and C++

Web Technology - Neamat El Tazi 19

---

# Slide 20: JavaScript Objects
[Image Description: A slide titled "JavaScript Objects"]
- JavaScript is object-oriented and uses the same method-calling syntax as Java
- We have already seen this with the document object

Code Example:
document.write("Hello World!");

Web Technology - Neamat El Tazi 20

---

# Slide 21: Built-In JavaScript Objects
[Image Description: A slide titled "Built-In JavaScript Objects"]
- Some basic objects are built-in to JavaScript
  - String
  - Date
  - Array
  - Boolean
  - Math

Web Technology - Neamat El Tazi 21

---

# Slide 22: JavaScript Strings
[Image Description: A slide titled "JavaScript Strings"]
- A String object is created every time you use a string literal (just like in Java)
- Have many of the same methods as in Java
  - charAt, concat, indexOf, lastIndexOf, match, replace, search, slice, split, substr, substring, toLowerCase, toUpperCase, valueOf
- There are also some HTML specific methods
  - big, blink, bold, fixed, fontcolor, fontsize, italics, link, small, strike, sub, sup
- Don't use the HTML methods (use CSS instead)
  - This is the worst kind of visual formatting

Web Technology - Neamat El Tazi 22

---

# Slide 23: JavaScript Dates
[Image Description: A slide titled "JavaScript Dates"]
- The Date class makes working with dates easier
- A new date is initialized with the current date
- Dates can be compared and incremented

Code Example:
var myDate = new Date();
myDate.setFullYear(2007,2,14);

var today = new Date();
var nextWeek = today + 7;

if (nextWeek > today) {
  alert("You have less than one week left");
}

Web Technology - Neamat El Tazi 23

---

# Slide 24: JavaScript Arrays and Booleans
[Image Description: A slide titled "JavaScript Arrays and Booleans"]
- We have already seen the Array class
- The Boolean class encapsulates a boolean value

Web Technology - Neamat El Tazi 24

---

# Slide 25: The JavaScript Math Class
[Image Description: A slide titled "The JavaScript Math Class"]
- The Math class encapsulates many commonly-used mathematical entities and formulas
- These are all class methods
  - abs, acos, asin, atan, atan2, ceil, cos, exp, floor, log, max, min, pow, random, round, sin, sqrt, tan
- These are all class properties (Note: slide text says 'methods' but lists constants)
  - E, LN2, LN10, LOG2E, LOG10E, PI, SQRT1_2, SQRT2

Code Example:
if (Math.cos(Math.PI) != 0) {
  alert("Something is wrong with Math.cos");
}

Web Technology - Neamat El Tazi 25

---

# Slide 26: JavaScript and the DOM
[Image Description: A slide titled "JavaScript and the DOM"]
- The Document Object Model (DOM) is a specification that determines a mapping between programming language objects and the elements of an HTML document
- Not specific to JavaScript

Web Technology - Neamat El Tazi 26

---

# Slide 27: HTML DOM Objects
[Image Description: A slide titled "HTML DOM Objects"]
- Environment objects
  - Window, Navigator, Screen, History, Location, Document
- HTML objects
  - Anchor, Area, Base, Body, Button, Event, Form, Frame, Frameset, Iframe, Image, Checkbox, FileUpload, Hidden, Password, Radio, Reset, Submit, Text, Link, Meta, Object, Option, Select, Style, Table, TableCell, TableRow, TextArea

Web Technology - Neamat El Tazi 27

---

# Slide 28: HTML DOM: Document
[Image Description: A slide titled "HTML DOM: Document"]
- The Document object represents an HTML document and can be used to access all documents in a page
- A Document contains several collections
  - anchors, forms, images, links
- Has several properties
  - body, cookie, domain, lastModified, referrer, title, URL
- Has several useful methods
  - getElementById, getElementsByName, getElementsByTagName, write, writeln, open, close

Web Technology - Neamat El Tazi 28

---

# Slide 29: HTML DOM: Document
[Image Description: A slide titled "HTML DOM: Document"]
- An instance of Document is already created for you, called document

Code Example:
function changeF() {
  var cText = document.getElementById("c");
  var fText = document.getElementById("f");
  ...
}

...

<input type="text" id="c" onchange="changeC()">C
<input type="text" id="f" onchange="changeF()">F

Web Technology - Neamat El Tazi 29

---

# Slide 30: HTML DOM: Form Elements
[Image Description: A slide titled "HTML DOM: Form Elements"]
- One of the most common uses of JavaScript is for form validation
- Several HTML DOM classes encapsulate form elements
  - Form, Button, Checkbox, Hidden, Password, Text, Radio, Reset, Submit, Textarea

Web Technology - Neamat El Tazi 30

---

# Slide 31: HTML DOM: Text
[Image Description: A slide titled "HTML DOM: Text"]
- A text entry field (input type="text") is encapsulated by a Text object
- Variables
  - value, maxLength, id, size, name, tabindex, readOnly
- Changing these variables has an immediate effect on the displayed data

Web Technology - Neamat El Tazi 31

---

# Slide 32: HTML DOM: The Document Tree
[Image Description: A slide titled "HTML DOM: The Document Tree"]
- Accessing elements and changing their properties lets us do simple things like form validation, data transfer etc
- HTML DOM lets us do much more
- We can create, delete, and modify parts of the HTML document
- For this we need to understand the Document Tree

Web Technology - Neamat El Tazi 32

---

# Slide 33: HTML DOM: The Document Tree
[Image Description: A hierarchical tree diagram showing the HTML DOM structure. 
At the top is "Document". 
Below it is "Root element: <html>". 
The <html> element branches into "Element: <head>" and "Element: <body>". 
The <head> element branches into "Element: <title>", which further branches into "Text: "My title"". 
The <body> element branches into "Element: <a>" and "Element: <h1>". 
The <a> element has an attached "Attribute: "href"" and branches into "Text: "My link"". 
The <h1> element branches into "Text: "My header"".]

Web Technology - Neamat El Tazi 33

---

# Slide 34: Navigating the Document Tree
Check our previous Lecture for Navigating and Updating the DOM.

[Image Description: A slide titled "Navigating the Document Tree"]
- With JavaScript we can navigate the document tree
- document.getElementById(), getElementsByName(), and getElementsByTagName() return nodes in the document tree

Web Technology - Neamat El Tazi 34

---

# Slide 35: History
[Image Description: A timeline diagram showing the history of ECMAScript versions.
- 1997: ECMAScript 1 (First edition)
- 1998: ECMAScript 2 (Editorial changes only)
- 1999: ECMAScript 3 (Regular Expressions, try/catch)
- (Timeline skips forward) ECMAScript 4 (Never released)
- 2009: ECMAScript 5 (strict mode, JSON support, String.trim(), Array.isArray(), Array Iteration Methods)
- 2015: ECMAScript 6]

https://medium.com/@bluetch/javascript-es6-es7-es8-es9-es10-es11-and-es12-519d8be7d48c
Newest Version: ES14 (ECMAScript 2023)
ECMAScript 2024, the 15th and current version (standard), was released in June 2024
Web Technology - Neamat El Tazi 35

---

# Slide 36: Outline
[Image Description: A slide titled "Outline"]
- Data types
- Arrays
- Functions
- Objects

Web Technology - Neamat El Tazi 36

---

# Slide 37: Data Types
[Image Description: A slide titled "Data Types"]
- There is only one kind of variable and it is declared with the var keyword
- Basic values are
  - Numbers, like 42 and 3.14159
  - Booleans, true and false
  - Strings, like "thanks for all the fish..." and "have a good day"
  - null
  - undefined, the value of a variable declared by never assigned to

Code Example:
var answer = 42;
answer = "thanks for all the fish...";

Web Technology - Neamat El Tazi 37

---

# Slide 38: Type Conversion
[Image Description: A slide titled "Type Conversion"]
- Expressions involving a string, the + operator and a number will convert the numbers to strings

Code Example:
x = "The answer is " + 42
// returns "The answer is 42"

y = 42 + " is the answer"
// returns "42 is the answer"

- With other operators, strings are converted to numbers

Code Example:
"37" - 7 // returns 30
"37" + 7 // returns "377"

Web Technology - Neamat El Tazi 38

---

# Slide 39: Type Conversion (2)
[Image Description: A slide titled "Type Conversion (2)"]
- Boolean values
  - null and undefined treated as false
  - 0 treated as false
  - "" (the empty string) treated as false
  - Everything else treated as true

Web Technology - Neamat El Tazi 39

---

# Slide 40: Variables
[Image Description: A slide titled "Variables"]
- A variable can be declared using the var keyword or simply assigned to
- The scope of a variable is limited to the containing block

Code Example:
var x = 42;
var q;          // q = undefined
y = 75;
if (condition) {
  var z = 411;
}
// z no longer in scope

Web Technology - Neamat El Tazi 40

---

# Slide 41: Global Variables
[Image Description: A slide titled "Global Variables"]
- Global variables are properties of the global object
- In web pages, this object is called window
- We can use the window.variable syntax to do this
- We can access a global variable in another window if we know the name of the other window
- This can be a security risk

Web Technology - Neamat El Tazi 41

---

# Slide 42: Global Variables
- You can access global variables through any the following objects:
  - window
  - self
  - this
  - frames
  - globalThis (standard way right now to access a global variable in all environments)
- window, self, frames will not work in Node.js environment
Check Demo 8
https://blog.logrocket.com/what-is-globalthis-why-use-it/
Web Technology - Neamat El Tazi 42

---

# Slide 43: Constants
[Image Description: A slide titled "Constants"]
- Read-only named constants can be created with the const keyword
- Scoping rules for constants are exactly the same as for variables
- Constants live in the same name space as variables and function names

Code Example:
const pi = 3.14159;

Web Technology - Neamat El Tazi 43

---

# Slide 44: Array Literals
[Image Description: A slide titled "Array Literals"]
- Array literals can be specified with the [] syntax
- Adding extra commas creates undefined array entries
- This actually creates an Array object

Code Example:
var fish = ["lion", "angel", "grouper"];

var places = ["home", , "school", "work"];
places = ["home", undefined, "school", "work"]

Web Technology - Neamat El Tazi 44

---

# Slide 45: More Literals
[Image Description: A slide titled "More Literals"]
- Strings
  - single- or double-quoted
- Boolean
  - true and false
- Integers
  - specified in decimal, octal, or hexadecimal
- Floating-point Literals
  - in the usual ways

Web Technology - Neamat El Tazi 45

---

# Slide 46: Functions
[Image Description: A slide titled "Functions"]
- JavaScript functions can be created using the function keyword
- Functions can return a value using the return keyword (or return undefined by default)

Code Example:
function factorial(n) {
  if ((n == 0) || (n == 1))
    return 1;
  else {
    var result = (n * factorial(n-1));
    return result;
  }
}

Web Technology - Neamat El Tazi 46

---

# Slide 47: Functions - Weirdness
Code Example:
c = function () {alert("this is a function without a name")};

How can we call this function? ☺
This is called a function expression instead of function declaration

[Image Description: A slide titled "Functions - Weirdness"]
- Scope rules for functions are the same as for variables
- Functions don't have to have names
- Functions can be assigned to variables or object properties

Web Technology - Neamat El Tazi 47

---

# Slide 48: Function Arguments
[Image Description: A slide titled "Function Arguments"]
- JavaScript is very flexible with function arguments
- A function can be called with more or less arguments than the number of declared parameters
- Too few arguments: leaves parameters undefined

Code Example:
function showThese(x, y) {
  document.write(x + "\n");
  document.write(y + "\n");
}
showThese("hello"); // prints hello and undefined

Web Technology - Neamat El Tazi 48

---

# Slide 49: Functions - arguments
[Image Description: A slide titled "Functions - arguments"]
- All the arguments to a function can be accessed through the (implicit) arguments pseudo-array

Code Example:
function printThese () {
  for (var i = 0; i < arguments.length; i++) {
    document.writeln(arguments[i]);
  }
}
printThese("a", "b", "c", "d", 42);

Web Technology - Neamat El Tazi 49

---

# Slide 50: eval
- Warning: Avoid eval() in production code
- Security risk — executes arbitrary code
- Performance impact — cannot be optimized by JS engine

[Image Description: A slide titled "eval"]
- The eval function evaluates a string as if it were JavaScript code
- The evaluation environment is the same as that in which eval is called

Code Example:
var myCode = "document.writeln(x);"

var x = 56;
eval(myCode);    // prints 56

Web Technology - Neamat El Tazi 50

---

# Slide 51: Object Literals
[Image Description: A slide titled "Object Literals"]
- JavaScript has objects but these are not what you're used to
- They are closer to C structs or Perl hashes than Java objects

Code Example:
var employee = {
  FirstName: "Patrick",
  LastName: "Morin",
  eId: 244333433
};

document.write(employee.LastName + ", "
             + employee.FirstName + ", "
             + employee.eId);

Web Technology - Neamat El Tazi 51

---

# Slide 52: Objects
Check Demo 9

[Image Description: A slide titled "Objects"]
- The values in an object are usually called properties
- Property names can also be numbers

Code Example:
var employee = {
  1: "Patrick",
  2: "Morin",
  eId: 244333433
};

document.write(employee[1] + ", "
             + employee[2] + ", "
             + employee.eId);

Web Technology - Neamat El Tazi 52

---

# Slide 53: Accessing Object Properties
[Image Description: A slide titled "Accessing Object Properties"]
- Object properties can be accessed using the . or [] operators

Code Example:
var car = { color: "red",
            weight: 2000,
            mfr: "Hyundai",
            cost: 21000,
            1: "dohc",
            2: "abs" };

document.write(car.color); // "red"
document.write(car["color"]); // "red"
document.write(car[color]); // ERR: color undefined
var prop = "color";
document.write(car[prop]); // "red"

Web Technology - Neamat El Tazi 53

---

# Slide 54: Operators
[Image Description: A slide titled "Operators"]
- We have already seen familiar operators, but these are new:
  - delete
  - in
  - instanceof
  - new
  - this
  - typeof
  - void

Web Technology - Neamat El Tazi 54

---

# Slide 55: delete
[Image Description: A slide titled "delete"]
- delete removes something from a name space
- Can remove an object, a property, or an element at an index
- Future accesses to that will evaluate to undefined

Code Example:
delete objectName
delete objectName.property
delete objectName[index]

- Can remove an implicitly declared variable, but not one declared using the var keyword

Web Technology - Neamat El Tazi 55

---

# Slide 56: Question
- var a = 10;
- b = 20;
- console.log(window.a); // ?
- console.log(window.b); // ?
- console.log(delete a); // ?
- console.log(delete b); // ?
- console.log(a); // ?
- console.log(b); // ?

Web Technology - Neamat El Tazi 56

---

# Slide 57: Answer
- window.a // 10
- window.b // 20
- delete a // false
- delete b // true
- a // 10
- b // ReferenceError

Web Technology - Neamat El Tazi 57

---

# Slide 58: in
[Image Description: A slide titled "in"]
- The in operator determines whether an object has a certain property or an array has a certain index

Code Example:
if ("cost" in car) {
  document.write("Cost: " + car.cost + "\n");
}

if (23 in a) {
  document.write("twenty-third: " + a[23]);
}

Web Technology - Neamat El Tazi 58

---

# Slide 59: typeof
[Image Description: A slide titled "typeof"]
- The typeof operator returns a string representing the type of the argument
- Can be one of
  - "function"
  - "string"
  - "number"
  - "object"
  - "undefined"

Code Example:
document.write(typeof(car)); // "object"

Web Technology - Neamat El Tazi 59

---

# Slide 60: void
[Image Description: A table titled "void"]
| Link | Action Taken | Use |
| :--- | :--- | :--- |
| void(0) | Nothing happens | Dummy link placeholder |
| void(document.form.submit()) | Submits a form | Executes logic avoids page reload |

[Image Description: A slide titled "void"]
- The void operator specifies an expression to evaluate without returning a value
- Useful within an href attribute:

Code Example:
<a href="javascript:void(0)">Do nothing</a>

<a href="javascript:void(document.form.submit())">
Click here to submit
</a>

Web Technology - Neamat El Tazi 60

---

# Slide 61: Objects and Classes
[Image Description: A slide titled "Objects and Classes"]
- JavaScript doesn't really have classes
- Instead, you define a constructor function that sets the properties of the implicit variable "this"

Code Example:
function Cell(r, c, val) {
  this.row = r;
  this.col = c;
  this.value = val;
}
var c = new Cell(5, 4, "Priscilla");
document.write("c.row = " + c.row + "\n");
document.write("c.col = " + c.col + "\n");
document.write("c.value = " + c.value + "\n");

Web Technology - Neamat El Tazi 61

---

# Slide 62: instanceof
[Image Description: A slide titled "instanceof"]
- The instanceof keyword tests if an object is of a specific class (created by a constructor with a specific name)
- This really checks if the object was created using the named constructor function

Code Example:
var c = new Cell(5, 4, "Priscilla");

if (c instanceof Cell) {
  document.write("c is a Cell");
}

Web Technology - Neamat El Tazi 62

---

# Slide 63: Prototypes
Prototype is the basis of JavaScript inheritance
Check Demo 10

[Image Description: A slide titled "Prototypes - Adding Properties"]
- Constructor functions have a property named prototype that allows for the creation of properties after the fact

Code Example:
Cell.prototype.width = 20;

var c1 = new Cell(0, 0, "treasure");
var c2 = new Cell(4, 0, "hunt");

c1.width = "40";
document.writeln("c2.width = " + c2.width);

Web Technology - Neamat El Tazi 63

---

# Slide 64: AI in JavaScript Development
- Writing JavaScript with AI:
  - Generate boilerplate: 'Write a JS class for a shopping cart with add, remove, total'
  - Debug: paste your error + code — AI identifies the issue and explains the fix
  - Refactor: 'Convert this callback-based code to async/await'
- AI tools for JavaScript:
  - GitHub Copilot — autocompletes functions, generates test stubs
  - Claude / ChatGPT — explain concepts, refactor, review logic, convert JS versions
  - Tabnine / Codeium — lightweight in-editor AI completion
- Good prompt pattern:
  - State task + constraints: 'Write a debounce function in vanilla JS, no dependencies'
Web Technology - Neamat El Tazi 64

---

# Slide 65: AI in JavaScript Testing
- Unit test generation:
  - Paste a function → ask AI to generate Jest / Mocha test cases including edge cases
  - GitHub Copilot auto-suggests test() blocks as you write code
- End-to-end testing:
  - Playwright + AI: describe user flows in plain English, AI writes the script
  - Testim / Mabl: AI-powered recorders that self-heal when UI changes
- Debugging & code review:
  - Paste a stack trace into Claude or ChatGPT — AI pinpoints the root cause
  - Prompt: 'Review this JS for security issues, performance, and best practices'
Web Technology - Neamat El Tazi 65

---

# Slide 66: What’s Coming in JavaScript
- ECMAScript 2015 (ES6) was the biggest update to JavaScript.
- Coming in the next lecture:
  - let and const — block-scoped variable declarations
  - Arrow functions — compact function syntax
  - JavaScript Classes — clean OOP over prototypes
  - Promises and async/await — asynchronous programming
  - Default parameters, Rest/Spread operators
  - ES7 through ES14 highlights
  - AJAX — Asynchronous JavaScript and XML
Web Technology - Neamat El Tazi 66